{
  "name": "feedback-board",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/shared",
    "server",
    "web"
  ],
  "scripts": {
    "verify": "npm run migrate --workspace=server && npm run test --workspace=server"
  }
}
